Problem Solution

- projectfolder conatins compute.py file which has the Python code for given problem.

Steps to run the program:

1. git clone https://github.com/FalgunMakadia/analyze-re.git
2. cd projectfolder
3. python ./compute.py {threshold_value} {limit_value}

- Program will ask for number of inputs you want to enter (max 100 inputs)
- Enter input count
- Enter all inputs one by one (Press Enter after each value)
- At the end of all the inputs, Output will be displayed

